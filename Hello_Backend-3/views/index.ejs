<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <!-- <link rel="stylesheet" href="../public/stylesheet/style.css"> -->
</head>
<body>
    <div class="maindiv bg-slate-600 w-full min-h-screen text-white p-5">
        <div class="form">
            <form action="/create" method="post">
                <input class="block  w-full mb-2 text-black px-2" type="text" name="heading">
                <textarea class="block  mb-2 text-black  w-full px-2" name="text" id=""></textarea>
                <input class="bg-blue-600 px-5 py-2" type="submit" name="" id="">
            </form>
        </div>
        <div class="task flex flex-wrap gap-3 ">

            
            <% if (file.length > 0) { %>
                <% file.forEach((val) => { %>
                    <div class="minitask  w-72 mt-10 bg-slate-700 p-2">
                        <h1 class="text-xl"><%= val %></h1>
                        <a href="/file/<%= val %>" class="text-blue-500">Learn more</a>
                    </div>
                <% }) %>
            <% } else { %>
                <h1 class="mt-20">No task yet.</h1>
            <% } %>
            
            
           
           
        </div>
    </div>
</body>
</html>